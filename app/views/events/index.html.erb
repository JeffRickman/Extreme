<p id="notice"><%= notice %></p>

<h1>Events</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Event date</th>
      <th>Address line1</th>
      <th>Address line2</th>
      <th>Leader name</th>
      <th>Leader phone</th>
      <th>Details</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <!-- Inserted -->
    Program Name<br />
    <%= f.select :program_id, @options %>
    <!--  Inserted -->

    <% @events.each do |event| %>
      <tr>
        <td><%= event.name %></td>
        <td><%= event.description %></td>
        <td><%= event.event_date %></td>
        <td><%= event.address_line1 %></td>
        <td><%= event.address_line2 %></td>
        <td><%= event.leader_name %></td>
        <td><%= event.leader_phone %></td>
        <td><%= event.details %></td>
        <td><%= event.status %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event', new_event_path %>
<!-- INSERTED -->
<p><%= link_to 'Programs', programs_path %></p>
<!-- INSERTED -->
